---
title: Docker
description: Deploy Fleet with Docker or Docker Compose.
---

## Docker Compose (recommended)

The repo includes a `docker-compose.yml` for the simplest deployment path.

```bash
git clone https://github.com/aayushxr/fleet.git
cd fleet
docker compose up -d
```

Fleet will be available at `http://localhost:3000`.

## Docker (standalone)

### Build the image

```bash
docker build -t fleet .
```

### Run the container

```bash
docker run -d \
  -p 3000:3000 \
  -e NODE_ENV=production \
  --name fleet \
  fleet
```

## Reverse proxy

To expose Fleet publicly, put it behind a reverse proxy (nginx, Caddy, etc.) that handles TLS.

### Caddy example

```
fleet.yourdomain.com {
  reverse_proxy localhost:3000
}
```

<Warning>
  Socket.IO requires sticky sessions if you run multiple Fleet instances behind a load balancer. For most self-hosted setups, a single instance is sufficient.
</Warning>

## Updating

```bash
git pull
docker compose up -d --build
```
